//// Page layout shell: wraps page content with the header, footer, and the
//// signature wavy boundary between sections.
////
//// Scaffold stub; see `ROADMAP.md` (Phase 3).

import lustre/element.{type Element}

/// Render the application shell around page content. Not yet implemented.
pub fn view(_content: Element(msg)) -> Element(msg) {
  todo as "layout.view: header + main(content) + footer + wavy boundary (ROADMAP Phase 3)"
}
