//// Post list view: renders a chronological list of posts with pagination,
//// mirroring apollo's `section.html` template.
////
//// Scaffold stub; see `ROADMAP.md` (Phase 4).

import lustre/element.{type Element}

/// Render a paginated list of posts. Not yet implemented.
pub fn view(_posts: List(a)) -> Element(msg) {
  todo as "post_list.view: chronological list + pagination (ROADMAP Phase 4)"
}
