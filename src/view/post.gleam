//// Single post view: renders a post's header, body, table of contents, tags,
//// and comments — mirroring apollo's `page.html` template.
////
//// Scaffold stub; see `ROADMAP.md` (Phase 4).

import lustre/element.{type Element}

/// Render a single post. Not yet implemented.
pub fn view(_post: a) -> Element(msg) {
  todo as "post.view: header + body + TOC + tags + comments (ROADMAP Phase 4)"
}
